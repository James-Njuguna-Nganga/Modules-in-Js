
const processOrder = (order) => {
  console.log(`Processing bulk order for ${order.quantity} ${order.productName}(s)`);
  // Additional processing logic for bulk orders
};

module.exports = {
  processOrder
};

const processOrder = (order) => {
  console.log(`Processing order for ${order.quantity} ${order.productName}(s)`);
  // Additional processing logic for default orders
};

module.exports = {
  processOrder
};
